import styled from 'styled-components'
import pokeball from '../../../../../../assets/pokeball.gif'


const Main = styled.div`
  height: 100%;
  width: auto;
  background: url(${pokeball}) no-repeat center;
  filter: opacity(0.1);
`

export { Main }
